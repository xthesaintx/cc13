{{#each entities as |entity|}}
  {{#if entity.questListHtml}}
    <div class="quest-group">
      <h3><i class="{{entity.icon}}">&nbsp;</i>{{entity.name}}</h3>
      {{{entity.questListHtml}}}
    </div>
  {{/if}}
{{/each}}